-- ============================================================
-- REBORN ART AI — Миграция 010: Премахване на старата order_id
-- Дата: 2026-06-21
-- ============================================================
-- Таблицата client_downloads вече има preview_order_id и
-- full_order_id (виж миграция 009, Секция 7b), които напълно
-- заместват старата order_id колона. Тъй като таблицата е
-- потвърдено празна, премахваме старото поле директно, без
-- нужда от поетапна миграция на данни.
-- ============================================================

BEGIN;

ALTER TABLE client_downloads DROP CONSTRAINT IF EXISTS client_downloads_order_id_fkey;
ALTER TABLE client_downloads DROP COLUMN IF EXISTS order_id;
DROP INDEX IF EXISTS idx_client_downloads_order_id;

COMMIT;

-- ============================================================
-- ПРОВЕРКА СЛЕД МИГРАЦИЯТА
-- ============================================================
-- \d client_downloads
-- Очакваме: order_id вече не съществува, само preview_order_id
-- и full_order_id остават като референции към поръчките.
-- ============================================================
